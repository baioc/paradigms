(define (square x) (* x x))
(define (double x) (+ x x))
(define (halve x) (/ x 2))
(define (average a b) (/ (+ a b) 2))

(define (print m) (display m) (newline))

(define (id x) x)

(define tolerance 1e-9)
